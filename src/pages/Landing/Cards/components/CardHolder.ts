import styled from '@emotion/styled'

const CardHolder = styled.div`
  padding: 4rem;
  background: #fff;

  display: flex;
  flex-flow: column;
  align-items: center;
  margin: 0 auto;
`

export { CardHolder }
